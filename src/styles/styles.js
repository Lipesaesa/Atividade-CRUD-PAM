import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flex: 1,
        padding: 20,
        marginTop: 40,
        backgroundColor: "#f5f7fb"
    },

    title: {
        fontSize: 26,
        fontWeight: "bold",
        marginBottom: 20
    },

    input: {
        borderWidth: 1,
        borderColor: "#d0d7de",
        backgroundColor: "#fff",
        padding: 12,
        borderRadius: 10,
        marginBottom: 12
    },

    buttonSpace: {
        marginBottom: 10
    },

    card: {
        padding: 15,
        borderWidth: 1,
        borderColor: "#ddd",
        backgroundColor: "#fff",
        marginTop: 10,
        borderRadius: 12
    },

    name: {
        fontSize: 18,
        fontWeight: "bold"
    },

    email: {
        fontSize: 14,
        marginTop: 4
    }
});